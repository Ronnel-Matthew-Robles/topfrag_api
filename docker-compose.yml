version: "3"

services:
  topfrag:
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - ./topfrag:/topfrag
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"
